# Run Application With Dock

- Build Application with Docker Compose

  ```bash
  cd path\to\project\directory
  ```

- Run Docker Compose

  ```bash
  docker compose up -d --build
  ```

- Access Application from Browser
  ```bash
  https:\\127.0.0.1:2455
  ```
